## Prerequisites

This project uses `sphinx-simplepdf` (via WeasyPrint), which requires system-level graphics libraries to render the PDFs.

**macOS (Homebrew):**
```bash
brew install pango libffi cairo